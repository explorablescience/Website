export const metadata = {
    title: 'ExplorableScience - Interactive Simulations'
}

export default function ClientLayout(props: { children: React.ReactNode }) {
    return <>{props.children}</>;
}

